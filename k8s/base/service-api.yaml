apiVersion: v1
kind: Service
metadata:
  name: hospital-api
  labels:
    app: hospital
spec:
  type: NodePort
  selector:
    app: hospital
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30080
